# Local development (optional if using DATABASE_URL)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=
DB_NAME=ecg_db
DB_PORT=5432

# Preferred for production / Vercel / Neon:
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
